#!/bin/bash
# Pre-commit hook to auto-sync global Stories index and sprint-status.yaml
# This ensures the Sprint-Story-Module Mapping and sprint-status.yaml are always up-to-date

# Check if any Story files changed
