# Examples

本目录提供 apprun 的反向代理配置示例文件。

## Reverse Proxy Configuration

Example at reverse-proxy directory: 

- `nginx.conf` - Nginx 反向代理配置
- `Caddyfile` - Caddy 反向代理配置（自动 HTTPS）
- `traefik.yml` - Traefik Docker Compose 配置（自动 HTTPS）

## 使用方式

根据实际环境选择合适的反向代理方案，复制对应配置文件并修改域名、证书路径等参数。
